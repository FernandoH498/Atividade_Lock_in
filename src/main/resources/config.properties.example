db.url=jdbc:mysql://localhost:3306/achados_perdidos?useSSL=false&serverTimezone=America/Sao_Paulo&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
db.user=SEU_USUARIO
db.password=SUA_SENHA

# Gmail SMTP — use uma Senha de App do Google (nao sua senha normal)
# Ativar: myaccount.google.com → Seguranca → Verificacao em 2 etapas → Senhas de app
mail.smtp.host=smtp.gmail.com
mail.smtp.port=587
// Exemplo simples de como o Java busca a variável de ambiente ou o arquivo
String email = System.getenv("MAIL_USER") != null ? 
               System.getenv("MAIL_USER") : 
               prop.getProperty("mail.smtp.user");
